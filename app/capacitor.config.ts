import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cf.furmeds',
  appName: 'fur-meds',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
